process SKIM = {

  untracked PSet configurationMetadata = {
    untracked string version = "$Revision: 1.2 $"
    untracked string name = "$Source: /cvs_server/repositories/CMSSW/CMSSW/Configuration/Skimming/data/PDElectron_Skims6.cfg,v $"
    untracked string annotation = "skims to be run on PDElectron, file 3/3"
  }

  untracked PSet options = {
         untracked vstring FailPath = { "ProductNotFound" }
         untracked bool wantSummary = true
         untracked bool makeTriggerResults = true 
  }

  untracked PSet maxEvents = {untracked int32 input = -1}

  include "FWCore/MessageLogger/data/MessageLogger.cfi"

  source = PoolSource {
    untracked vstring fileNames = {
   '/store/RelVal/2007/9/3/RelVal-RelValZTT-1188837971/0002/0279DD81-655A-DC11-8280-0016177CA778.root'
    } 
  }


  ### TopQuarkAnalysis
  # skim: topDiLepton2Electron
  include "TopQuarkAnalysis/TopSkimming/data/topDiLepton2Electron_SkimPaths.cff"
  include "TopQuarkAnalysis/TopSkimming/data/topDiLepton2ElectronOutputModule.cfi"


  endpath TopQuarkAnalysis = {
    topDiLepton2ElectronOutputModule  
  }
}
