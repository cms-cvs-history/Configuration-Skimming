process SKIM = {

  untracked PSet configurationMetadata = {
    untracked string version = "$Revision: 1.3 $"
    untracked string name = "$Source: /cvs_server/repositories/CMSSW/CMSSW/Configuration/Skimming/data/PDBJet_Skims0.cfg,v $"
    untracked string annotation = "skims to be run on PDBJet"
  }

  untracked PSet options = {
         untracked vstring FailPath = { "ProductNotFound" }
         untracked bool wantSummary = true
         untracked bool makeTriggerResults = true 
  }

  service = LoadAllDictionaries {}

  untracked PSet maxEvents = {untracked int32 input = -1}

  include "FWCore/MessageLogger/data/MessageLogger.cfi"

  source = PoolSource {
    untracked vstring fileNames = {
      '/store/mc/2007/9/17/RelVal-W1jet_100ptw300-alpgen-1190037682/0002/0AFADF7A-3565-DC11-BCE6-000423D944DC.root'
    } 
  }


  ### TopQuarkAnalysis
  # skim: topFullyHadronic
  include "TopQuarkAnalysis/TopSkimming/data/topFullyHadronic_SkimPaths.cff"
  include "TopQuarkAnalysis/TopSkimming/data/topFullyHadronicOutputModule.cfi"
  endpath TopQuarkAnalysis = {topFullyHadronicOutputModule}

}
